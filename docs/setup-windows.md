# Windows Setup

## Dotfiles

```bash
./sync.sh diff windows    # show differences
./sync.sh apply windows   # apply configs
./sync.sh save windows    # save system configs back
```

## Reference

For full setup guide (installation, tools, media workflow), see:
- https://github.com/hi-ogawa/windows-setup
